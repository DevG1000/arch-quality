# arch-quality 架构改进建议（基于最近一次标准架构质量评估）

> **评估基线**：综合 55.4 / 100（结构 59.97 / 设计 75.04 / 文档 33.25 / 演进 48.0）
> **日期**：2026-08-21
> **提分预估**：全部落地后综合 55.4 → ~78.7（+23.3）

---

## P0（立即处理，最高优先级）

### P0-1 拆分 4 个 God File（引擎模块化）
**预估提分**：+4.2（结构）+1.25（设计）≈ **+5.5 综合**

| 文件 | 行数 | 拆分方案 | 目标 |
|:-----|:----:|:---------|:-----|
| `arch_metrics_multilang.py` | 1833 | 拆 `mlr_rules.py` + `fortran_support.py` | <600 行 |
| `arch_metrics_solver_physics.py` | 1502 | 拆 `coupling_detect.py` + `plugin_detect.py` | <600 行 |
| `arch_metrics_standard.py` | 1291 | 拆 `doc_metrics.py` + `evo_metrics.py` | <700 行 |
| `arch_metrics_template.py` | 1166 | 拆 `template_rules.py` | <600 行 |

- 收益：复杂度 0→~70（SAR-004 解除）、反模式 70→90（SAR-006 解除）、内聚提升
- 风险：需回归测试守护（`ARCH_REGRESSION_UPDATE=1` 对比基线）

### P0-2 补文档体系（CHANGELOG + ADR）
**预估提分**：**+5.4 综合**（文档 33.25→60.2）

- 创建 `CHANGELOG.md`：keepachangelog 格式（`## [版本] - 日期` + Added/Fixed）→ CHANGELOG 0→100
- 建立 `docs/adr/` + `.opencode/templates/adr.md`：记录 3-5 个关键架构决策（测试覆盖度 4 层算法、SAR 单一检测源、多语言性能优化方案）→ ADR 0→60
- 同时解除 SAR-010（文档缺失）

### P0-3 控制提交粒度（小步提交）
**预估提分**：**+3.2 综合**（演进 48.0→60.8）

- 现状：`avg_commit_size(30) = 1311 行`（大爆炸式提交），增量质量 0 分
- 动作：变更按逻辑拆分（<300 行/次），文档/代码/测试分离提交
- 收益：增量质量 0→80，演进质量显著提升

---

## P1（重要，短期解决）

### P1-1 建立债务趋势基线
**预估提分**：**+3.0 综合**（演进 60.8→72.8）

- 现状：无 `history.json` 历史基线 → 债务趋势 0 分
- 动作：连续运行 2 次评估建立基线（工具已自动保存历史），对比结构质量趋势
- 收益：债务趋势 0→60

### P1-2 README 补全
**预估提分**：**+2.5 综合**（文档 60.2→72.8）

- 现状：README 35 分（有简介/安装/使用，缺功能/配置/贡献）
- 动作：补 `## 功能`（评估维度矩阵）、`## 配置`（--build-dir/--test-dirs/--report-mode）、`## 贡献`
- 收益：README 35→85

### P1-3 L1 测试覆盖平铺识别（工具改进）
**预估提分**：**+1.7 综合**（结构 74.0→79.8）

- 现状：源码单层 `src/` + 平铺 `tests/` → L1 仅 5.9%（模块级一一对应语义不匹配单层结构）
- 动作：L1 增加"平铺布局识别"（单层 src ↔ 顶层 tests 视为覆盖），排除 `docs/zh/mms/*` 脚本目录分母
- 收益：测试覆盖度 51→80，SAR-005 解除

---

## P2（优化，中期）

### P2-1 注释密度优化
**预估提分**：**+0.6 综合**（文档 72.8→75.8）

- 现状：注释比例约 5-10%（下限区间）
- 动作：为 `arch_core.py`、`arch_report_generator.py` 公共方法补文档字符串
- 收益：注释密度 40→60

### P2-2 架构风格识别增强
**预估提分**：**+0.75 综合**（设计 80.0→83.0）

- 现状：架构风格 60.24（弱主导），只识别 plugin 风格
- 动作：补全指南 4.3 的六风格检测（分层/模块化/事件驱动/MVC/微内核/整洁架构），增强依赖方向分析
- 收益：风格 60→75

### P2-3 多语言 O(n²) 性能重构
**预估提分**：**+0.67 综合**（结构 79.8→82.0）+ 性能 KPI 达标

- 现状：`arch_metrics_multilang.py` 4 处 O(n²) 热点（WP-0 定位），FreeCAD src 全量 4h
- 动作：ctypes 匹配/PyImport 解析/SWIG 扫描改为哈希索引
- 收益：耦合度 68.78→80 + FreeCAD 全量 P90 <10min（KPI2）

---

## 执行顺序与依赖

```
阶段 1（P0，1-2 周）: P0-2 → P0-3 → P0-1
  文档/提交习惯先行（低风险高收益），引擎拆分需回归守护
阶段 2（P1，1-2 周）: P1-1 → P1-2 → P1-3
阶段 3（P2，持续）: P2-1 → P2-2 → P2-3（P2-3 与 WP-3 性能优化协同）
```

## 预期结果

| 阶段 | 累计综合 | 变化 |
|:-----|:--------:|:----:|
| 当前 | 55.4 | — |
| 阶段 1（P0）| ~67.7 | +12.3 |
| 阶段 2（P1）| ~74.7 | +7.0 |
| 阶段 3（P2）| ~78.7 | +4.0 |

> 全部落地后：综合 55.4 → ~78.7（"需改进" → "良好"），SAR-004/005/006/010 全部解除，性能 KPI2 达标。